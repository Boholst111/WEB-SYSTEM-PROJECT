name: CI/CD Pipeline

# CI/CD pipeline is temporarily disabled
# To re-enable: uncomment the 'on' section below and remove the workflow_dispatch trigger

# on:
#   push:
#     branches: [ main, develop, staging ]
#   pull_request:
#     branches: [ main, develop ]

on:
  workflow_dispatch:  # Only run manually from Actions tab

jobs:
  placeholder:
    name: CI/CD Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Information
        run: |
          echo "=========================================="
          echo "CI/CD Pipeline is Temporarily Disabled"
          echo "=========================================="
          echo ""
          echo "The CI/CD pipeline has been disabled to prevent continuous failures."
          echo ""
          echo "To re-enable:"
          echo "1. Uncomment the 'on' section at the top of this file"
          echo "2. Remove the 'workflow_dispatch' trigger"
          echo "3. Ensure your tests pass locally first"
          echo "4. Commit and push the changes"
          echo ""
          echo "For local testing:"
          echo "  Backend: cd backend && php artisan test"
          echo "  Frontend: cd frontend && npm test"
          echo ""
          echo "=========================================="
